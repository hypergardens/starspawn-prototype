Entity
    baseName: string 
    ? parent: ID
    ? rel: string

Signal
    type: string
    ? key: value

Action
    ? func: string
    ? args: value[] 
    ? duration: number
    ? pause: number
    ? signals: Signal[]
    ? processText: string

    c: maxDuration: number
    c: timeStarted: number

Intent
    representation: Entity[]
    sequence: Action[]

Word < Entity

Command: Word[]

Actor < Entity
    intent: null | Intent

Pattern
    intents() => Intent[]
